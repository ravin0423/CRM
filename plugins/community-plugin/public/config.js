// Per-deployment settings. Replace these before shipping — the defaults
// below are for local development against mock-console/host.html only.
window.__COMMUNITY_PLUGIN_CONFIG__ = {
  apiBase: "http://localhost:8787/api/plugin",
  allowedHostOrigins: ["*"], // e.g. ["https://console.your-suremdm-domain.com"]
};
